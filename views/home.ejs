<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
</head>

<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>User List</h2>
            <a href="/add" class="btn btn-primary">
                <i class="bi bi-person-add"></i>Add New Contact</a>
        </div>
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <td>S.No.</td>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- Ejs syntax -->
                 <% var sno=1 %>
                <% contacts.forEach(contact=>{%>
                <tr>
                    <td><%=sno++%></td>
                    <td><%=contact.first_name %></td>
                    <td><%=contact.last_name %></td>
                    <td><%=contact.email %></td>
                    <td><%=contact.phone %></td>
                    <td>
                        <a href="/show/<%=contact._id %>" class="text-info me-2" title="Show">
                            <i class="bi bi-eye-fill"></i>
                        </a>
                        <a href="/update/<%=contact._id %>" class="text-warning me-2" title="Edit">
                            <i class="bi bi-pencil-fill"></i>
                        </a>
                        <a href="/delete/<%=contact._id %>" class="text-danger me-2" title="Delete">
                            <i class="bi bi-trash-fill"></i>
                        </a>
                    </td>
                </tr>
                <%});%>
            </tbody>
        </table>
    </div>

    <script src="bootstrap.js"></script>
</body>

</html>